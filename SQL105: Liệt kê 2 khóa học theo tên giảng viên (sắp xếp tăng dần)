SELECT 
       i.username,
       c.dept,
       c.number,
       c.title
FROM Instructor i
JOIN Teaches t 
  ON i.username = t.username
JOIN Class c 
  ON t.dept = c.dept 
 AND t.number = c.number
ORDER BY i.lname ASC, i.fname ASC
LIMIT 2;
